${one}
${one}${two}
${n}
${false}
${true}
${html}
${html2}
${html3}
${safeHtml}
${html|raw}
${html2|raw}
${html3|raw}
${name}
${name|capitalize}
${name|upper}
${nameUpper|lower}
${nameUpper|lower|capitalize}
${objt|json}
${objt|json|raw}
${objt|raw|json}
${objt|capitalize|json|raw}
${objt|upper|json|raw}
${objt|lower|json|raw}
${objn|lower|json|raw}
${one + 1}
${one|add(1)}
${one|incr}
${one|decr}
${one|subtract(1)}
${one|mul(5)}
${three|div(2)}
${name|cut("iw")}
${objt|cut("a")|json|raw}
${objn|add(-2)|json|raw}
${objn|add(5)|json|raw}
${objn|subtract(2)|json|raw}
${objn|mul(17)|json|raw}
${objn|div(2)|json|raw}
${html2|addslashes|raw}
${html3|addslashes|raw}
${html2|addslashes|stripslashes|raw}
${html3|addslashes|stripslashes|raw}
${name|first}
${arr|first}
${objt|first}
${name|last}
${arr|last}
${objt|last}
${arr|join("#")}
${objm|join("#")}
${arr|reverse|join("#")}
${objm|reverse|join("#")}
${objm|length}
${arr|length}
${name|length}
${html|urlencode}
${html|urlencode|urldecode|raw}
${date|timeago}
${oldDate|date("dddd, MMMM Do YYYY, h:mm:ss a")}
${name|replace('k','n')}
${name|replace('K','w')}
${name|replace('k','w','i')}
${float1|round}
${float2|round}
${float1|floor}
${float2|floor}
${float1|ceil}
${float2|ceil}

{{html one}}
{{html html3|raw}}
{{html html3|escape}}